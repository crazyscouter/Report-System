report = {};

report.ReportCoolDown = 30 //minutes; how many a player must way before reporting again
report.SaveInterval = 30 //in seconds
report.GeneralReasons = {
	"Random Death Match",
	"Hurting me :(",
	"Being a general asshat",
	"Scrub"
}
report.AdminGroups = {
	"supersexyadmin",
}
report.SuperAdminGroups = {
	"developer",
}

report.BracketColor = Color(0, 0, 0, 255);
report.ReportTextColor = Color(255, 25, 25, 255);
report.MsgColor = Color(255, 255, 255, 255);